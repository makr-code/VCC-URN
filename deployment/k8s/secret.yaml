---
apiVersion: v1
kind: Secret
metadata:
  name: vcc-urn-secrets
  namespace: default
type: Opaque
stringData:
  # Database credentials (example - replace with actual values)
  URN_DB_URL: "postgresql://urn:changeme@postgres:5432/urn"
  # API Keys (example - replace with actual values)
  URN_API_KEYS: "dev-key-1:admin,dev-key-2:reader"
  # OIDC Configuration (optional)
  # URN_OIDC_ISSUER: "https://issuer.example/realms/realm"
  # URN_OIDC_AUDIENCE: "api://urn-resolver"
  # URN_OIDC_JWKS_URL: "https://issuer.example/realms/realm/protocol/openid-connect/certs"
